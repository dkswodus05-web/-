# 실거래 전 안전 체크리스트 (Phase 6 준비)

> ⚠️ 아래 항목을 **전부** 만족하기 전에는 `config.py`의 `PAPER`를 `False`로 두지 않는다.
> 이건 투자 자문이 아니며, 자동매매는 실제 손실을 낼 수 있다. 모든 판단과 책임은 본인에게 있다.

마지막 실증: 2026-06-26 (DRY_RUN 시뮬 로그로 검증)

---

## A. 자동으로 실증 완료된 항목 ✅

각 안전장치가 실제로 매수를 막거나 보류하는지 DRY_RUN 로그로 하나씩 확인했다.

| 안전장치 | 테스트 시나리오 | 기대 | 결과 |
|----------|----------------|------|------|
| **킬 스위치** | `KILL_SWITCH=True` + BUY 90 | 매수 차단 | ✅ "⛔ 차단 — 킬 스위치 ON" |
| 킬 스위치(청산) | 같은 상태 + SELL (보유 중) | 청산은 허용 | ✅ "🔴 전량 매도 전송" |
| **일일 손실 한도** | 당일 -2.5% + BUY 90 | 매수 차단 | ✅ "⛔ 차단 — 손실 한도(2%) 도달" |
| 손실 한도(경계) | 당일 -1.5% + BUY 90 | 통과(한도 미달) | ✅ "✅ 허용 — 매수" |
| **정규장 체크** | `market_open=False` + BUY 90 | 주문 보류 | ✅ "⏸ 정규장 마감 — 주문 보류" |
| 확신도 임계값 | BUY 45 (기준 60 미만) | 매수 보류 | ✅ (Phase 3 검증 시 확인) |
| 신호 신선도 | 24시간 초과 신호 | 안전 정지 | ✅ (Phase 3 검증 시 확인) |
| 파일/형식 오류 | 없거나 깨진 signal.json | 안전 정지 | ✅ (Phase 3 검증 시 확인) |

**결론: 코드 레벨 안전장치는 의도대로 작동한다.**

---

## B. 본인이 직접 확인해야 하는 항목 ⬜

실제 환경·계좌·시간에 묶여 있어 사람이 직접 해야 한다.

- [ ] **킬 스위치를 실제로 눌러** 운영 중 정지되는 걸 확인
      → `config.py`에서 `KILL_SWITCH = True`로 바꾸고 `python run.py BUY 90` → 차단 로그 확인
- [ ] **Alpaca 페이퍼 키 연동** — `.env`에 키 넣고 `DRY_RUN=false` → 실제 모의주문이 Alpaca에 들어가는지 확인
- [ ] **페이퍼로 최소 수 주 운영** — 매일 스케줄(평일 8시)이 무사히 도는지 며칠~몇 주 관찰
- [ ] **일일 손실 한도가 실제 계좌 손익으로 매수를 막는지** 로그로 확인 (시뮬 아닌 실제 last_equity 기준)
- [ ] **장 마감·휴장·API 장애 시 멈추는지** 실제 휴장일/장 마감 후 실행해 확인
- [ ] **백테스트 결과를 납득** — Buy&Hold 대비 성과·MDD를 숫자로 이해 (→ `python backtest.py`)
- [ ] **잃어도 생활에 지장 없는 금액**만 넣는다
- [ ] **이 모든 책임이 나에게 있음**을 이해한다

---

## C. 실거래 전환 절차 (B를 전부 통과한 후에만)

1. `.env`에 Alpaca **페이퍼**(아직 실거래 아님) 키 입력, `DRY_RUN=false`로 며칠 모의주문 검증
2. 위 B 항목을 전부 체크
3. 그 후에만, 신중히 `config.py`의 `PAPER = False` (실거래) — **최소 금액으로**
4. 첫 며칠은 매일 `trades.log`와 실제 계좌를 대조하며 관찰

> 백테스트(Phase 5) 없이, 그리고 이 체크리스트 없이 실거래로 가지 않는다.
> 시스템의 8할은 "AI가 틀려도 계좌가 안 터지게"이다.
