# Signal Invest Pro — 프로젝트 지침서 (2026-07-10 개정)

> 이 문서는 프로젝트 설정(지침)에 붙여넣는 원본이다.
> 새 대화가 이 프로젝트를 정확히 이해하는 데 필요한 전부를 담는다.
> 코딩을 몰라도 AI에게 "무엇을 만들지" 설명할 수 있으면 만들 수 있다.

---

## 0. 이 프로젝트가 만드는 것

AI가 미국 증시의 매크로 지표 19개를 분석하고, 세 명의 AI 에이전트(Bull/Bear/Judge)가 토론해
**신호·확신도·레짐(5단계 시장 국면)**을 판정하면, 안전장치를 거쳐
**5종목 유니버스(SPY·QQQ·QLD·TQQQ·GLD)를 레짐별 목표 비중으로 자동 리밸런싱**하는 시스템.
Railway 클라우드에서 평일 밤 11:35(한국시간, 14:35 UTC = 미국 장중)에 전자동 실행되고,
결과는 공개 대시보드와 텔레그램 브리핑으로 온다.

**현재 모드: PAPER(모의투자).** Alpaca 가짜 돈 계좌($100,000 시작, 7/2~)이며 진짜 돈은 움직이지 않는다.

---

## 1. 대원칙 (절대 어기지 말 것)

1. **이건 투자 자문이 아니다.** 신호는 도구의 출력일 뿐, 모든 판단과 책임은 본인에게 있다.
2. **AI는 틀린다.** "AI를 똑똑하게"보다 "AI가 틀려도 계좌가 안 터지게"가 시스템의 8할이다.
3. **브로커도 틀린다.** 2026-07-07 Alpaca가 멀쩡한 계좌를 "포지션 없음"으로 잘못 응답한 사건 이후,
   브로커 응답도 검증 대상이다 (계좌 급변 감지).
4. **모의투자(Paper)부터.** 실거래는 충분한 모의 운영 후, 최소 금액으로만.
5. **안전 기본값.** 새 코드의 기본 상태는 항상 "주문 안 나감" 쪽이어야 한다.
6. **데이터 오류 시 즉시 멈춤.** 지표를 못 구하면, AI 응답이 깨지면, 계좌가 이상하면 — 그날은 아무것도 안 한다.

---

## 2. 핵심 결정 (Decision Log)

| 항목 | 결정 | 비고 |
|------|------|------|
| 시장/브로커 | 미국 주식 / Alpaca Paper | 소수점 주식(금액 지정 주문) 지원 |
| 전략 | **레짐 리밸런싱** (Phase 7, 7/8 가동) | 단일 종목 스위칭(구 전략)을 대체 |
| 유니버스 | SPY·QQQ·QLD·TQQQ·GLD | ⚡QLD(2x)·TQQQ(3x) 레버리지 포함은 **본인의 명시적 결정(7/4)** — 페이퍼 한정 |
| 매매 주기 | 일 1회 | 매크로 지표는 느리게 움직임. 주기 변경은 백테스트 재검증 필요 |
| AI | Claude API **하루 1회 호출** (Bull/Bear/Judge를 한 응답에) | 확신도는 AI의 주관 점수 — 문턱(60) 용도로만 사용 |
| 데이터 | FRED(공식) + Yahoo, 19개 지표 | 하나라도 못 구하면 안전 정지 |
| 확신도 보정 | 성적표(performance_log) 축적 → 데이터로 문턱 조정 | AI에게 과거 사례 재주입은 안 함 (편향 증폭 위험) |
| 향후 실거래 | 한국투자증권 KIS API 검토 (한국인 계좌 개설 용이) | broker.py만 교체하면 됨. 정수 주 단위 주문으로 사이징 수정 필요 |

---

## 3. 매일 밤 일어나는 일 (파이프라인)

```
⏰ 평일 23:35 KST — Railway 크론 (프로젝트 "main")
 ① fred_collector.py   지표 19개 수집 (FRED+Yahoo, 실패 시 안전 정지)
 ② ai_committee.py     Claude API 1회 호출 → Bull/Bear 토론 + Judge의 신호·확신도·레짐
 ③ signal_io.py        signal.json 기록 (검증 실패 시 안전 정지)
 ④ rebalancer.py       🛡 안전 검문소 → 레짐별 목표 비중 vs 현재 보유 → ±5%p 밴드 밖만 매매
 ⑤ publish_status.py   모든 기록을 Gist에 저장 → 대시보드 갱신
 ⑥ telegram_notifier   데일리 브리핑(지표·토론·판결·보유) 폰으로 전송, 오류 시 경고
```

**안전 검문소(매수 차단 조건)**: 킬스위치 ON / 확신도 < 60 / 당일 손실 -2% 도달 /
**계좌 급변 감지**(자산 ±15% 급변 또는 매도 없는 보유 소실 → 매수·매도 전체 중단 + 텔레그램 경고).
매도(위험 축소)는 급변 감지 외에는 항상 허용.

**레짐별 목표 비중** (`config.py REGIME_WEIGHTS`, 합계 미달분은 현금):
공격 90% 위험자산(TQQQ 포함) / 균형 70% / 중립 50% / 방어 30% / 위기 10%(GLD만).

---

## 4. 운영 인프라

| 무엇 | 어디 | 비고 |
|------|------|------|
| 크론 실행 | Railway 프로젝트 "main" (id a3b845a3) | `35 14 * * 1-5` UTC. Cron Runs 탭에서 "Run now" 수동 실행 가능 |
| 대시보드 호스팅 | Railway 프로젝트 "서브" → web-production-34a77.up.railway.app | index.html을 공개 서빙 |
| 영구 데이터 | GitHub Gist `d6e06c315bb709e6533ee3a369684761` | Railway 컨테이너는 휘발성 — 남길 건 전부 여기 |
| 텔레그램 | 봇 Zeze93bot, 환경변수 TELEGRAM_BOT_TOKEN/CHAT_ID | Railway Variables에 설정됨 |
| 비밀값 | Railway Variables (11개) | 코드·깃·채팅에 키를 절대 넣지 말 것 |

**Gist 파일**: `performance_log.json`(성적표 — 매일 지표 스냅샷+판단+결과, 1000건),
`account_history.json`(잔고 추이 365건), `trade_history.json`(매매 500건),
`trades.log`(로그 300줄), `signal.json`/`indicators.json`(오늘 것, 덮어씀).

---

## 5. 파일 지도

| 파일 | 역할 |
|------|------|
| `daily_pipeline.py` | 총지휘자 — 전 단계 실행 + Gist 저장 + 성적표 축적 |
| `fred_collector.py` | 지표 19개 수집 |
| `ai_committee.py` | Claude API로 Bull/Bear/Judge (+레짐, +Alpha 브리핑) |
| `rebalancer.py` | 리밸런싱 트레이더 (현행 경로) |
| `executor.py` | 단일 종목 스위칭 (REBALANCE_MODE=false일 때의 구 경로) |
| `sanity.py` | 계좌 급변 감지 (양쪽 경로 공용) |
| `risk.py` / `config.py` | 리스크 게이트 / 모든 설정값 |
| `broker.py` | Alpaca 창구 (KIS 전환 시 이 파일만 교체) |
| `telegram_notifier.py` | 데일리 브리핑·에러 알림 |
| `publish_status.py` | Gist 업로드 |
| `index.html` | 공개 대시보드 (Gist를 읽어 렌더링, 이상치 필터·포트폴리오 모달 포함) |
| `backtest.py` | 백테스트 (10년: 전략 +150%/MDD -22% vs 매수후보유 +324%/-33.7%) |

---

## 6. 이 프로젝트에서 일하는 방식 (중요 — 세션 공통 규칙)

1. **푸시는 사람이 한다.** Claude가 파일을 수정하면, 사용자가 GitHub Desktop으로 커밋·푸시한다.
   푸시하면 Railway가 자동 재배포된다. Claude는 푸시 자동화를 시도하지 않는다 (과거 실패).
2. **푸시 확인은 `git log`/`git show HEAD:<파일>`로.** `git status`/`git diff`는 믿지 않는다
   (bash 마운트가 수정 직후 파일을 잘리거나 낡은 상태로 보여주는 고질적 캐시 문제 — Read 도구가 항상 정답).
3. **Run now는 용도에 맞게.** 장 마감 중 실행은 주문 코드 경로를 타지 않는다.
   매매 로직 검증은 미국 장중(한국 밤 10:30~새벽 5시)에만 의미 있다.
   변수 변경 직후의 재배포와 겹치면 실행이 중간에 죽는다 — 배포 ACTIVE 확인 후 실행.
4. **Alpaca 웹사이트에서 Tax ID·주민번호 등 신분정보 입력 절대 금지.** (Live 계좌 KYC로 리다이렉트되는 경우 있음)
5. **한 번에 한 파일, DRY_RUN 정신.** 수정 → 검증(컴파일·시뮬) → 푸시 → 실행 확인 순서.

---

## 7. 실거래 전 체크리스트 (전부 통과 전 PAPER=False 금지)

- [x] 백테스트 완료 (Phase 5)
- [x] 페이퍼 실운영 시작 (7/2~) — **몇 주 이상 지켜보기 진행 중**
- [ ] 킬 스위치를 실제로 켜서 매수 차단 확인
- [ ] 일일 손실 한도 작동을 로그로 확인
- [x] 장 마감·휴장·API 장애 시 멈춤 확인 (7/3 휴장, 7/7 글리치로 실증)
- [ ] 잃어도 되는 최소 금액만 / 책임은 본인에게

---

## 8. 다음 단계 (급하지 않음)

1. **성적표 채점** — performance_log 50건(두어 달) 쌓이면 확신도별 적중률 계산 → MIN_CONFIDENCE 데이터 보정
2. **킬 스위치 실전 테스트** — 실거래 검토 전 필수
3. **KIS 전환 검토** — 실거래 결정 시 (모의계좌 → broker.py 교체 → 재검증)
4. (선택) 장중 위기 감시 실행 — 매도만 허용하는 2차 실행. 레버리지 보유 중 급락 대비

---

### 고지
본 프로젝트는 개인 학습·기록용이다. 투자 권유가 아니며, 자동매매는 실제 손실을 낼 수 있다.
레버리지 ETF는 손익이 증폭된다. 모든 판단과 책임은 이용자 본인에게 있다.
