# 자동매매 켜는 법 — 단계별 가이드

> ⚠️ 이 가이드는 **자동매매를 켤 수 있는 상태**까지 안내합니다.
> 실제로 켜는 것(실주문·실거래 전환)은 본인이 직접, 본인 책임으로 합니다.
> AI(저)는 트레이드를 대신 실행하거나 돈을 움직이지 않습니다.

지금 시스템은 3단계 모드가 있습니다. **아래로 갈수록 위험**하니, 반드시 위 단계를 충분히 거친 뒤 내려가세요.

```
DRY_RUN (연습, 지금)  →  Paper (모의·가짜돈)  →  Live (실거래·진짜돈)
   주문 안 나감             진짜처럼 모의주문         진짜 돈
```

---

## 0단계 (지금) — DRY_RUN, 아무것도 안 바꿔도 됨

매일 8시 스케줄러가 돌면서 신호를 만들고 "샀다고 치자"만 기록합니다.
- `signal_page.html`로 오늘 신호를 눈으로 확인
- `dashboard.html`로 그동안의 기록 확인
- **이 상태로 최소 수 주 관찰하세요.** 신호가 말이 되는지 보는 단계입니다.

---

## 1단계 — Paper(모의투자)로 진짜처럼 주문 넣기

가짜 돈이지만 Alpaca 서버에 실제로 주문이 들어갑니다. 체결·잔고가 진짜처럼 움직여요.

**(1) Alpaca 페이퍼 키 발급**
1. https://app.alpaca.markets/ 가입
2. 우상단에서 **Paper** 계정 선택 (절대 Live 아님)
3. API Keys 생성 → Key와 Secret 복사

**(2) `.env`에 키 입력**
`.env` 파일을 열어 채웁니다:
```
ALPACA_API_KEY=발급받은_키
ALPACA_SECRET_KEY=발급받은_시크릿
DRY_RUN=false        ← 여기를 false로 (연습 해제)
```
> `PAPER`는 config.py에서 `True`로 그대로 둡니다. 이게 "가짜 돈" 안전판입니다.

**(3) 패키지 설치 후 테스트**
```bash
pip install -r requirements.txt
python run.py BUY 72        # 모의계좌에 실제 매수 주문이 들어가는지 확인
```
Alpaca 페이퍼 계좌 화면에서 주문이 보이면 성공입니다.

**(4) 며칠~몇 주 모의 운영**
스케줄러가 매일 도는 동안 `trades.log`와 Alpaca 화면을 대조하세요.

---

## 2단계 — Live(실거래). 절대 서두르지 말 것

`SAFETY_CHECKLIST.md`의 항목을 **전부** 통과했을 때만.
- 백테스트 결과를 납득했다 (`python backtest.py`)
- Paper로 수 주간 무사히 돌렸다
- 킬 스위치·손실한도가 실제로 막는 걸 확인했다
- 잃어도 생활에 지장 없는 금액만 넣는다

그 후에만, `config.py`에서 `PAPER = False`. **최소 금액으로 시작.**

> 이 전환은 가이드가 떠먹여 주지 않습니다. 본인이 위험을 이해하고 직접 결정하세요.

---

## 별도 질문: "컴퓨터를 항상 켜둬야 하나?"

네. 지금 구조는 **데스크톱 앱이 켜져 있을 때만** 스케줄이 돕니다.
앱을 꺼두면 그동안 멈췄다가, 다시 켤 때 밀린 작업이 한 번 실행됩니다.

**24시간 자동으로 돌리려면** 컴퓨터 대신 항상 켜진 서버에 올려야 합니다:
- 무료: Oracle Cloud Free Tier, 또는 라즈베리파이(집에 두고 24시간)
- 유료(간편): Railway, Render 등 (참고한 ZEZE 페이지도 Railway)
- 서버에 올리면 `cron`(리눅스 자동 실행기)으로 매일 정해진 시각에 파이썬을 돌립니다.

이건 다음 단계로 따로 도와드릴 수 있습니다 (코드는 그대로, 실행 장소만 옮기는 작업).

---

## 한눈에: 지금 당장 할 수 있는 가장 안전한 다음 행동

1. `signal_page.html` 열어 오늘 신호 확인
2. DRY_RUN 상태로 며칠 더 관찰
3. 준비되면 위 **1단계(Paper)** 만 진행 — 가짜 돈이라 안전
4. Live는 한참 뒤, 체크리스트 통과 후에만
